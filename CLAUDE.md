# tax-knowledge — 取扱規範

このリポジトリは**税法の条文と法令解釈通達の原文の機械的な写し**である。
e-Gov法令API v2 と国税庁サイトから自動取得しており、**解釈・要約・判断を一切含まない。**

## 最重要の注意

> [!danger] このリポジトリは public である
> **顧問先の実名・数値・資料・レポートを、いかなる形でも置いてはならない。**
> commit した時点でインターネットに公開され、削除しても履歴から復元できる。
>
> 顧問先データは別の場所（職員PCのローカル、または所内の共有）で扱う。
> `.gitignore` で `clients/ input/ output/ *.csv *.pdf *.xlsx` を除外してあるが、
> **除外設定に頼らず、そもそも置かないこと。**

## このリポジトリに置いてよいもの

| 置いてよい | 置いてはならない |
|---|---|
| APIから取得した条文原文 | **AIが書いた要約・解説・判断** |
| 法令ID・リビジョンID・施行日 | 顧問先の実名・数値・資料 |
| 収集スクリプトと設定 | 事務所固有の判定ルール（別リポジトリ） |
| 出典表示 | 市販の実務書・専門誌の本文 |

**AIが書いた要約を置いてはならない理由**は、人の検証を経ない要約は、
条番号ごと誤っていても一見もっともらしく読めるためである。
このリポジトリの価値は「検証が要らない原文であること」に尽きる。

---

## 条文の引き方

### 1. 索引から法令IDを得る

`index.json` に収録法令の一覧がある。略称（法法・消法・措法など）で引ける。

### 2. パスを組み立てる

```
law/<法令ID>/articles/At_<条番号>.md
```

| 引きたいもの | パス |
|---|---|
| 法人税法22条 | `law/340AC0000000034/articles/At_22.md` |
| 法人税法22条の2 | `law/340AC0000000034/articles/At_22_2.md` |
| 措置法67条の5（少額減価償却資産） | `law/332AC0000000026/articles/At_67_5.md` |
| 消費税法30条（仕入税額控除） | `law/363AC0000000108/articles/At_30.md` |
| 消費税法9条（小規模事業者の免税） | `law/363AC0000000108/articles/At_9.md` |

条番号が分からないときは `law/<法令ID>/README.md` の索引（条番号＋見出し）を Grep する。
見出しで探せるので「少額減価償却資産」のような論点名から引ける。

### 主な法令ID

| 法令 | 略称 | 法令ID |
|---|---|---|
| 法人税法 | 法法 | `340AC0000000034` |
| 法人税法施行令 | 法令 | `340CO0000000097` |
| 法人税法施行規則 | 法規 | `340M50000040012` |
| 消費税法 | 消法 | `363AC0000000108` |
| 消費税法施行令 | 消令 | `363CO0000000360` |
| 消費税法施行規則 | 消規 | `363M50000040053` |
| 租税特別措置法 | 措法 | `332AC0000000026` |
| 租税特別措置法施行令 | 措令 | `332CO0000000043` |
| 租税特別措置法施行規則 | 措規 | `332M50000040015` |
| 国税通則法 | 通則法 | `337AC0000000066` |
| 所得税法 | 所法 | `340AC0000000033` |
| 所得税法施行令 | 所令 | `340CO0000000096` |
| 所得税法施行規則 | 所規 | `340M50000040011` |

### 3. 通達の引き方

```
tsutatsu/<コード>/items/<通達番号>.md
```

| 引きたいもの | パス |
|---|---|
| 法基通2-2-14（短期の前払費用） | `tsutatsu/hojin-kihon/items/2-2-14.md` |
| 法基通7-8-3（少額又は周期の短い費用） | `tsutatsu/hojin-kihon/items/7-8-3.md` |
| 法基通9-2-9（経済的な利益） | `tsutatsu/hojin-kihon/items/9-2-9.md` |
| 措通(法)61の4(1)-1（交際費等の意義） | `tsutatsu/sochiho-hojin/items/61の4(1)-1.md` |
| 措通(法)67の5-1 | `tsutatsu/sochiho-hojin/items/67の5-1.md` |
| 消基通1-1-1（個人事業者と給与所得者の区分） | `tsutatsu/shohi-kihon/items/1-1-1.md` |

| 通達 | コード | 略称 | 項目数 |
|---|---|---|--:|
| 法人税基本通達 | `hojin-kihon` | 法基通 | 1,364 |
| 租税特別措置法関係通達（法人税編） | `sochiho-hojin` | 措通(法) | 1,826 |
| 消費税法基本通達 | `shohi-kihon` | 消基通 | 653 |
| 所得税基本通達 | `shotoku-kihon` | 所基通 | 608 |

**通達番号が分からないときは `tsutatsu/<コード>/README.md` を Grep する。**
通達番号と見出しの一覧なので、「短期の前払費用」「交際費等の意義」のように
論点名から番号を引ける。全文検索より速い。

> [!warning] 通達番号を記憶から書いてはならない
> 「法基通7-3-1の2は取得価額の判定」のような記述は、番号と内容がずれていても
> もっともらしく読める（実際の7-3-1の2は「借入金の利子」である）。
> **必ずファイルを開いて、見出しと本文を確認してから引用すること。**

### 4. 経過措置は附則にある

**インボイス制度の2割特例・80%／50%控除の経過措置は、消費税法の本則ではなく
改正法の附則にある。** 消費税法の `suppl/` に改正法ごとのファイルとして入っている。

| 論点 | 場所 |
|---|---|
| 2割特例 | `law/363AC0000000108/suppl/平成二八年三月三一日法律第一五号.md` の附則51条の2 |
| 免税事業者からの仕入れの経過措置 | 同ファイル内 |

附則は改正法ごとに1ファイルなので、**論点名で Grep する**のが速い。

---

## 適用年度の扱い ★重要

> [!warning] このリポジトリには現在施行されている版しか入っていない
> 過去の事業年度の取引を判断するときは、**その時点で施行されていた条文**が必要である。
> リポジトリの条文をそのまま使ってはならない。

過去の時点の条文は、APIを直接引いて取得する。

```
https://laws.e-gov.go.jp/api/2/law_data/<法令ID>?elm=Mp-At_<条番号>&asof=YYYY-MM-DD
```

例: 2024年4月1日時点の法人税法22条

```
https://laws.e-gov.go.jp/api/2/law_data/340AC0000000034?elm=Mp-At_22&asof=2024-04-01
```

`asof` を付けた応答も数KBで済む。認証は不要である。

**回答には、根拠とした条文の施行日を必ず書く。**
リポジトリの条文を使った場合は `law/<法令ID>/_meta.json` の
`amendment_enforcement_date` がその施行日である。

---

## 含まれていないもの

| | 状況 |
|---|---|
| **通達**（法基通・措通(法)・消基通・所基通） | **収録済み（4,451項目）** |
| 国税通則法基本通達 | **未対応。** 通達番号を持たず「第◯条関係」＋見出しのみの構造のため |
| 措置法通達（消費税編ほか間接税） | **未対応。** 個別通達で見出しに番号が入る別構造のため |
| **質疑応答事例・タックスアンサー** | **未収録。** 国税庁サイトから別途取り込む予定 |
| **判例・裁決** | **未収録。** 裁判所・国税不服審判所から取り込む予定 |
| **会計基準**（企業会計原則・ASBJ基準・中小指針） | **未収録。** 民間団体の著作物は著作権法13条の対象外のため、本文を置けない。参照と自作要約で別扱いにする |
| 相続税法・地方税法・電子帳簿保存法・印紙税法 | 法令IDは確定済み。`config/laws.json` の `有効` を `true` にすれば収録される |

**未収録のものを「記憶から」書いてはならない。** 判例の日付や未対応の通達番号を推測で書くことは、
このリポジトリを作った目的に正面から反する。未収録であれば、
国税庁サイト等を実際に参照し、参照したURLと取得日を明示すること。

---

## 生成物を手で編集しない

次のファイルは収集スクリプトが生成する。**手で直しても次回の実行で消える。**

| ファイル | 生成元 |
|---|---|
| `law/` 配下のすべて・`index.json`・`LAWS.md` | `collectors/egov_law.py` |
| `tsutatsu/` 配下のすべて | `collectors/nta_tsutatsu.py` |

直したい場合はスクリプトを直す。整形の不具合を見つけたら、
`render_*` 関数を修正して全体を再生成する。

## 収集スクリプトに手を入れるときの約束

1. **標準ライブラリのみを使う。** `pip install` を要求すると GitHub Actions と各PCで差が出る。
2. **取得日時をファイルに書かない。** git のコミット日時が取得日時である。
   本文に日時を入れると、改正が無い日にも差分が出て履歴が太る。
3. **本文が変わらないファイルを書き換えない**（`write_if_changed` を必ず通す）。
4. **条数とファイル数の整合検査を外さない。** 条番号に使えない文字（`136:137` のようなコロン）が
   混じると条文が無言で失われる。実際に一度起きている。
5. **AIを使う処理を入れない。** このスクリプトはスケジュール実行だけで完結すべきである。
6. **索引はディスク上の全件から作り直す。** 「今回収集した分」だけで索引を上書きすると、
   `--law` や `--code` で1件だけ実行したときに他が索引から消える。実際に一度起きている。
7. **国税庁サイトを辿るときは間隔を空ける**（現在0.8秒）。`max_pages` の上限も外さない。
8. **日付を名前にしたディレクトリ（例 `/20230930/`）は旧版のアーカイブである。**
   基本通達では除外する。拾うと廃止前の本文が現行として混ざる。実際に一度混ざっている。

---
※ このリポジトリは条文を提供するだけである。税務判断は行わない。
※ 最終判断は担当税理士が行ってください。
